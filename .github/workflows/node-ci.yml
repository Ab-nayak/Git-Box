name: Node CI/CD - Pass Demo

on:
  push:
    branches: [Learn-CI]

jobs:
  test-app:
    runs-on: ubuntu-latest

    steps:
    - name: 🛎️ Checkout repository
      uses: actions/checkout@v3

    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'

    - name: 📦 Install dependencies
      run: npm install

    - name: ✅ Run test script
      run: npm test
